class DirectionsDetails {
  String distanceValue;
  String durationValue;
  String distanceText;
  String durationText;
  String encodePoints;

  DirectionsDetails(
      {this.distanceValue, this.durationValue, this.distanceText, this.durationText});

}